# 创建空元组
tup1 = (1, 2)
tup2 = (3, 4)

# 元组合并
tup = tup1 + tup2
print(tup)

# 计算元组元素个数
print(len(tup1))

# 返回元组中元素最大值。
print(max(tup))

# 返回元组中元素最小值。
print(min(tup))

# 将列表转换为元组。
print(tuple(["1", "2", "3"]))

# 删除元组
del tup